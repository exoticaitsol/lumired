      <section
        class="hero_banner_about bg_style"
        style="background-image: url('{{ section.settings.banner | img_url: 'master' }}')"
      >
        <div class="container">
          <div class="hero_txt_about">
            <div class="hero_txt_under">
              <h1>{{ section.settings.title }}</h1>
            </div>
          </div>
        </div>
      </section>

      <section class="nav_tab_sec py_9">
        <div class="container">
          <ul class="nav nav-pills" id="pills-tab" role="tablist">
            <li class="nav-item" role="presentation">
              <button
                class="nav-link active"
                id="pills-home-tab"
                data-bs-toggle="pill"
                data-bs-target="#pills-home"
                type="button"
                role="tab"
                aria-controls="pills-home"
                aria-selected="true"
              >
                General
              </button>
            </li>
            <li class="nav-item" role="presentation">
              <button
                class="nav-link"
                id="pills-profile-tab"
                data-bs-toggle="pill"
                data-bs-target="#pills-profile"
                type="button"
                role="tab"
                aria-controls="pills-profile"
                aria-selected="false"
              >
                Treatment
              </button>
            </li>
            <li class="nav-item" role="presentation">
              <button
                class="nav-link"
                id="pills-contact-tab"
                data-bs-toggle="pill"
                data-bs-target="#pills-contact"
                type="button"
                role="tab"
                aria-controls="pills-contact"
                aria-selected="false"
              >
                Order & Returns
              </button>
            </li>
          </ul>
          <div class="tab-content" id="pills-tabContent">
            <div
              class="tab-pane fade show active"
              id="pills-home"
              role="tabpanel"
              aria-labelledby="pills-home-tab"
            >
              <div class="accordian_sec accordian_content">
                <div
                  class="accordion accordion-flush"
                  id="accordionFlushExample"
                >
                 {% for block in section.blocks %}
                 {% if block.type == 'tab_one' %}
                  <div class="accordion-item">
                    <h2 class="accordion-header" id="flush-heading{{forloop.index0}}">
                      <button
                        class="accordion-button collapsed"
                        type="button"
                        data-bs-toggle="collapse"
                        data-bs-target="#flush-collapse{{forloop.index0}}"
                        aria-expanded="false"
                        aria-controls="flush-collapse{{forloop.index0}}"
                      >
                        {{ block.settings.title }}
                      </button>
                    </h2>
                    <div
                      id="flush-collapse{{forloop.index0}}"
                      class="accordion-collapse collapse"
                      aria-labelledby="flush-heading{{forloop.index0}}"
                      data-bs-parent="#accordionFlushExample"
                      style=""
                    >
                      <div class="accordion-body">
                      {{ block.settings.answer }}
                      </div>
                    </div>
                  </div>
                  {% endif %}
                  {% endfor %}
                </div>
              </div>
            </div>
            <div
              class="tab-pane fade"
              id="pills-profile"
              role="tabpanel"
              aria-labelledby="pills-profile-tab"
            >
              <div class="accordian_sec accordian_content">
                <div
                  class="accordion accordion-flush"
                  id="accordionFlushExample"
                >
                  {% for block in section.blocks %}
                  {% if block.type == 'tab_two' %}
                  <div class="accordion-item">
                    <h2 class="accordion-header" id="flush-heading{{forloop.index0}}">
                      <button
                        class="accordion-button collapsed"
                        type="button"
                        data-bs-toggle="collapse"
                        data-bs-target="#flush-collapse{{forloop.index0}}"
                        aria-expanded="false"
                        aria-controls="flush-collapse{{forloop.index0}}"
                      >
                        {{ block.settings.title }}
                      </button>
                    </h2>
                    <div
                      id="flush-collapse{{forloop.index0}}"
                      class="accordion-collapse collapse"
                      aria-labelledby="flush-heading{{forloop.index0}}"
                      data-bs-parent="#accordionFlushExample"
                      style=""
                    >
                      <div class="accordion-body">
                     {{ block.settings.answer }}
                      </div>
                    </div>
                  </div>
                  {% endif %}
                  {% endfor %}
               
                </div>
              </div>
            </div>
            <div
              class="tab-pane fade"
              id="pills-contact"
              role="tabpanel"
              aria-labelledby="pills-contact-tab"
            >
              <div class="accordian_sec accordian_content">
                <div
                  class="accordion accordion-flush"
                  id="accordionFlushExample"
                >
                  {% for block in section.blocks %}
                  {% if block.type == 'tab_three' %}
                  <div class="accordion-item">
                    <h2 class="accordion-header" id="flush-heading{{forloop.index0}}">
                      <button
                        class="accordion-button collapsed"
                        type="button"
                        data-bs-toggle="collapse"
                        data-bs-target="#flush-collapse{{forloop.index0}}"
                        aria-expanded="false"
                        aria-controls="flush-collapse{{forloop.index0}}"
                      >
                           {{ block.settings.title }}
                      </button>
                    </h2>
                    <div
                      id="flush-collapse{{forloop.index0}}"
                      class="accordion-collapse collapse"
                      aria-labelledby="flush-heading{{forloop.index0}}"
                      data-bs-parent="#accordionFlushExample"
                      style=""
                    >
                      <div class="accordion-body">
                          {% if request.host contains 'lumired.co.uk' and forloop.index0==17 %}
                       All our products are shipped from our Irish warehouse and will be dispatched within one working day after order confirmation. We use a range of couriers to ensure delivery of your product within 24-48 hours to Ireland and UK. European orders are received with 4-5 working days. Your tracking number will be sent by email as soon as your order has been dispatched.
                    {% else %}
                        {{ block.settings.answer }}
                    {% endif %}
                      </div>
                    </div>
                  </div>
                    {% endif %}
                    {% endfor %}
                
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

{% schema %}
  {
    "name": "FAQs",
    "settings": [
      {
        "type": "image_picker",
        "id": "banner",
        "label":"banner_image"
      },
     {
        "type": "text",
        "id": "title",
        "label":"Heading"
      },
           {
        "type": "text",
        "id": "tab1",
        "label":"Tab One"
      },
           {
        "type": "text",
        "id": "tab2",
        "label":"Tab two"
      },
           {
        "type": "text",
        "id": "tab3",
        "label":"Tab Three"
      }
    ],
    "blocks": [
    {
      "type": "tab_one",
      "name": "Tab One",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Add Question"
        },
           {
          "type": "richtext",
          "id": "answer",
          "label": "Add Answer"
        }
      ]
    },
       {
      "type": "tab_two",
      "name": "Tab two",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Add Question"
        },
                {
          "type": "richtext",
          "id": "answer",
          "label": "Add Answer"
        }
      ]
    },
       {
      "type": "tab_three",
      "name": "Tab Three",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Add Question"
        },
                {
          "type": "richtext",
          "id": "answer",
          "label": "Add Answer"
        }
      ]
    }
      ],
     "presets": [
       {
       "name": "FAQs"
       }
     ]
    
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}